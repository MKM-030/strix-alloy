# Vendored wheels and source tarballs fetched as benchmark evidence. Regenerable; far too large
# for Git. Keep the markdown reports and small JSON summaries.
wheels/
*.whl
*.tar.gz
*.zip
*.so

# Large live inventories regenerated by discovery runs.
*-inventory-live.json
*discovery-progress.jsonl
